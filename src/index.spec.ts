import { SERVICE_NAME } from './index';

describe('at1234-sectionb-2026-stpeterdemo1-levlimit-be', () => {
  it('should export SERVICE_NAME', () => {
    expect(SERVICE_NAME).toBe('at1234-sectionb-2026-stpeterdemo1-levlimit-be');
  });
});
